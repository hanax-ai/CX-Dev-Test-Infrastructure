# /opt/webui-gateway/gateway_app/config.yaml
# Single source of truth for model routing configuration
# Generated by Ansible deployment

model_map:
{% for backend in llm_backends %}
  # Models on {{ backend.name }} ({{ backend.host }})
{% for model in backend.models %}
  "{{ model }}": "{{ backend.host }}:{{ backend.port }}"
{% endfor %}
{% if not loop.last %}

{% endif %}
{% endfor %}
