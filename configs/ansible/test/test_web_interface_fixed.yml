---
- name: Test web-interface role syntax
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Include web-interface role
      include_role:
        name: web-interface
      vars:
        test_mode: true
